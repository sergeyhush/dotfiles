name: Bootstrap setup
on: [push]
jobs:
  build_and_test:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Full install
        run: ./bootstrap.sh
